<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/nav.css">
    <style>
        .post-header { border-bottom: 2px solid #ddd; padding-bottom: 10px; margin-bottom: 20px; }
        .post-info { font-size: 0.9em; color: #666; display: flex; gap: 15px; }
        .post-content { min-height: 200px; line-height: 1.6; margin-bottom: 30px; white-space: pre-wrap; }
        .btn-group { display: flex; gap: 10px; border-top: 1px solid #ddd; padding-top: 20px; }
    </style>
</head>
<body>
    <%- include('../partials/nav', { title: title, user: user }) %>
    <div class="container">
        <div class="post-header">
            <h2><%= post.title %></h2>
            <div class="post-info">
                <span>작성자: <%= post.author %></span>
                <span>작성일: <%= new Date(post.created_at).toLocaleString() %></span>
                <span>조회수: <%= post.views %></span>
            </div>
        </div>

        <div class="post-content"><%= post.content %></div>

        <div class="btn-group">
            <a href="/posts" class="button" style="background-color: #6c757d;">목록으로</a>
            
            <% if (user.user_id === post.user_id) { %>
                <a href="/posts/edit/<%= post.id %>" class="button">수정</a>
                <form action="/posts/delete/<%= post.id %>" method="POST" onsubmit="return confirm('정말 삭제하시겠습니까?');">
                    <button type="submit" class="delete">삭제</button>
                </form>
            <% } %>
        </div>
    </div>
</body>
</html>